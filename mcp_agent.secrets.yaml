# mcp_agent.secrets.yaml
openai:
  api_key: "ollama"  # Local Ollama - no external API needed
  base_url: "http://localhost:11434/v1"  # Local Ollama API endpoint

anthropic:
  api_key: ""  # Not needed for local setup
